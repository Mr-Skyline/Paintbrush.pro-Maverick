This folder is reserved for model training assets:

- datasets/ (raw + curated)
- labels/ (YOLO/segmentation annotations)
- scripts/ (fine-tuning jobs)

The initial scaffold focuses on deterministic CV baseline inference.
Model training hooks can be added in subsequent iterations.
